query getCharactersByPage($page: Int) {
    characters(page: $page) {
        info {
            pages
        }
        results {
            name
            species
            status
            gender
            image
        }
    }
}